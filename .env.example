# Anthropic API key — used by core/answer.ts (via cli.ts)
ANTHROPIC_API_KEY=

# Voyage AI API key — used for embedding at ingest time and query time
VOYAGE_API_KEY=

# Optional override for the answering model (defaults to claude-opus-4-8)
# ANTHROPIC_MODEL=claude-opus-4-8
